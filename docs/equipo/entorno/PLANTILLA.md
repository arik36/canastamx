# Entorno de trabajo · <clave> · <nombre>

<!--
Cópiate este archivo como docs/equipo/entorno/<tu clave>.md
Claves: a · b · c1 · c2 · d

Las versiones se copian de la salida REAL del comando. Inventarlas hace que
este documento sea peor que no tenerlo: en noviembre alguien va a depurar
contra una versión que nunca existió.

Este archivo se actualiza cuando instalas algo nuevo. Es lo que se consulta
la primera vez que alguien dice «en mi máquina sí funciona».
-->

## Máquina

- **Sistema operativo:**
- **Terminal que uso:**
- **Dónde vive el repositorio:**

## Herramientas

| Herramienta | Versión | Cómo la comprobé |
|---|---|---|
| git |  | `git --version` |
| gh |  | `gh --version` |

<!--
Agrega las de tu frente:

  A  · datos    python3 --version · pip --version
  B  · infra    docker --version · docker compose version
  C1 · dominio  java -version · mvn -v
  C2 · móvil    node --version · npm --version
  D  · web      node --version · navegador y versión

Si un comando dice «command not found», escribe «pendiente» y una nota de
cuándo lo vas a necesitar. Eso es información útil, no una falta.
-->

## Notas

<!--
Lo que otro integrante necesita saber para reproducir un problema tuyo.
Por ejemplo: que usas WSL y no Linux nativo, que tu Docker corre sin sudo,
que tu teclado es de otra distribución, o que trabajas detrás de un proxy.
-->
